{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RSSFeedItem",
  "description": "Industrial news RSS items that may impact consumption. Section A.3 of professor's spec.",
  "type": "object",
  "required": [
    "schema_version",
    "feed_id",
    "timestamp",
    "source",
    "category",
    "title",
    "summary",
    "impact_score",
    "keywords"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["1.0"]
    },
    "feed_id": {
      "type": "string",
      "pattern": "^RSS-[0-9]{8}-[0-9]{3}$",
      "description": "Unique feed item ID e.g. RSS-20260515-001"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "source": {
      "type": "string",
      "minLength": 3,
      "maxLength": 100,
      "description": "RSS source domain or publisher name"
    },
    "category": {
      "type": "string",
      "enum": ["INDUSTRIAL_NEWS", "REGULATION", "MARKET", "RENEWABLE", "INFRASTRUCTURE"],
      "description": "Categorical feature for ML correlation with consumption"
    },
    "title": {
      "type": "string",
      "minLength": 10,
      "maxLength": 200
    },
    "summary": {
      "type": "string",
      "minLength": 30,
      "maxLength": 1000,
      "description": "Article body — input for P2 NLP processing"
    },
    "impact_score": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Estimated impact on consumption (0=none, 1=major). Used by ML as feature weight."
    },
    "keywords": {
      "type": "array",
      "items": {"type": "string"},
      "minItems": 1,
      "maxItems": 10,
      "description": "Pre-extracted keywords for word cloud + correlation"
    }
  },
  "additionalProperties": false
}
